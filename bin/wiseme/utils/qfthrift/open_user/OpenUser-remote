#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py:new_style
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from open_user import OpenUser
from open_user.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  string ping()'
  print '  LoginResult mobile_login(i32 app_id, AuthInfo auth_info, LoginRecord login_info)'
  print '  LoginResult wx_login(i32 app_id, Profile profile, LoginRecord login_info)'
  print '  bool update_profile(i32 app_id, i32 user_id, string set_value)'
  print '  bool update_auth(i32 app_id, i32 user_id, string set_value)'
  print '  bool record_sms_code(i32 app_id, string mobile, string auth_code)'
  print '   get_profiles(i32 app_id, string spec)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = OpenUser.Client(protocol)
transport.open()

if cmd == 'ping':
  if len(args) != 0:
    print 'ping requires 0 args'
    sys.exit(1)
  pp.pprint(client.ping())

elif cmd == 'mobile_login':
  if len(args) != 3:
    print 'mobile_login requires 3 args'
    sys.exit(1)
  pp.pprint(client.mobile_login(eval(args[0]),eval(args[1]),eval(args[2]),))

elif cmd == 'wx_login':
  if len(args) != 3:
    print 'wx_login requires 3 args'
    sys.exit(1)
  pp.pprint(client.wx_login(eval(args[0]),eval(args[1]),eval(args[2]),))

elif cmd == 'update_profile':
  if len(args) != 3:
    print 'update_profile requires 3 args'
    sys.exit(1)
  pp.pprint(client.update_profile(eval(args[0]),eval(args[1]),args[2],))

elif cmd == 'update_auth':
  if len(args) != 3:
    print 'update_auth requires 3 args'
    sys.exit(1)
  pp.pprint(client.update_auth(eval(args[0]),eval(args[1]),args[2],))

elif cmd == 'record_sms_code':
  if len(args) != 3:
    print 'record_sms_code requires 3 args'
    sys.exit(1)
  pp.pprint(client.record_sms_code(eval(args[0]),args[1],args[2],))

elif cmd == 'get_profiles':
  if len(args) != 2:
    print 'get_profiles requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_profiles(eval(args[0]),args[1],))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
